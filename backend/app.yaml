runtime: custom
env: flex
env_variables:
  BUCKET_NAME: "stimson-gcs-ds-bucket"
  GOOGLE_APPLICATION_CREDENTIALS: "/app/.GOOGLE_APPLICATION_CREDENTIALS.json"
# https://cloud.google.com/appengine/docs/flexible/python/runtime
# entrypoint: gunicorn -b :$PORT app:app
# entrypoint: uwsgi --http :$PORT --wsgi-file app/app.py --callable app
runtime_config:
  python_version: 3
# [START handlers]
handlers:
  - url: /.*
    secure: always
    script: auto
# This sample incurs costs to run on the App Engine flexible environment. 
# The settings below are to reduce costs during testing and are not appropriate
# for production use. For more information, see:
# https://cloud.google.com/appengine/docs/flexible/python/configuring-your-app-with-app-yaml
manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 1.0
  disk_size_gb: 10
